<h1>みんなの腹ペコつぶやき一覧</h1>
<br>
<p style="color:red;"><%= notice %></p>
<table>
  <tr>
    <th>内容</th>
  </tr>
  
  <% @blogs.each do |blog| %>
    <tr>
      <td><%= blog.content %></td>
      <td><%= link_to '編集する', edit_blog_path(blog.id),
           data: { confirm: 'ホントに編集してもいいんですか？' } %></td>
      <td><%= link_to '削除する', blogs_path(blog.id),
           method: :delete, data: { confirm: 'ホントに消してもいいですか？' } %></td>
    </tr>
  <% end %>
</table>

<br>
<%= link_to '新しく腹ペコをつぶやいてみる', new_blog_path %>